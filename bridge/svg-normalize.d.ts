// Types for the dependency-free CJS helper the plugin bundle inlines (see svg-normalize.js).
export declare function normalizeSvgText(svg: string): string;
export declare function isSvgName(fileName: string): boolean;
